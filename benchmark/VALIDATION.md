# Benchmark Validation

The repository's structural validator is `scripts/validate-benchmark.sh` and runs in `.github/workflows/validate-benchmark.yml`.

Run locally with:

```bash
bash scripts/validate-benchmark.sh
```
