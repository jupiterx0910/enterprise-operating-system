# Benchmark Cases / Benchmark 案例

The flagship suite contains representative enterprise operating problems. Each case follows `../schema.md` and is scored with `../rubric.md`.

旗舰案例覆盖典型企业经营问题。每个案例遵循 `../schema.md` 并使用 `../rubric.md` 评分。
